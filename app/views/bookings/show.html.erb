<p id="notice"><%= notice %></p>

<fieldset>
  <legend>My bookings</legend>
</fieldset>


<%=  tabs_tag :open_tabs => {:class => "nav nav-tabs" }, :close_tabs => {:class => 'nav'} do |tab| %>
    <%= tab.now 'approaching',bookings_approaching_path %>
    <%= tab.history 'history', bookings_history_path %>
    <%= tab.all 'all bookings', bookings_all_path %>
<% end %>

<%=  render "bookingtable" %>